{
    "version": 2,
    "framework": "vite",
    "buildCommand": "cd frontend && rm -rf node_modules && npm install && npm run build",
    "outputDirectory": "frontend/dist",
    "installCommand": "npm install"
}